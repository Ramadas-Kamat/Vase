/**
 * Vase catalog barrel.
 *
 * TO ADD A VASE: add one `import './my-vase';` line below. Importing the module
 * is what registers it — the renderer, picker and share codec pick it up
 * automatically. Nothing else in the codebase needs to change.
 */
import './shapes';

// import './my-vase';   ← add new single-vase files here
