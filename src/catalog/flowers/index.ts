/**
 * Flower catalog barrel.
 *
 * TO ADD A FLOWER: add one `import './my-flower';` line below. Importing the
 * module is what registers it — the tray, properties panel, randomizer and
 * share codec pick it up automatically. Nothing else in the codebase changes.
 *
 * Order here is the order flowers appear in the tray (blooms are grouped before
 * fillers by `flowerTypesByCategory`).
 */
import './rose';
import './tulip';
import './daisy';
import './sunflower';
import './lavender';
import './blossom';
import './eucalyptus';
import './babysbreath';

// import './my-flower';   ← add new flowers here
